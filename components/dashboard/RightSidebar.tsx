import React, { memo } from "react";
import PropTypes from "prop-types";

const RightSideBar = () => {

  return (
    <div>
      <h1>Right Sidebar</h1>
    </div>
  )
} 

export default memo(RightSideBar);
